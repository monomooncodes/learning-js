//  variables is an identifer or a name which is used to refer a value.it is written with a combination of letter, number and special characters( underscore and dollor) where first letter begin with alphabates.

var  susie = "bca";
let  sta1 = "highlevel";
const name_01 = "running";

console.log(susie);
console.log(sta1);
console.log(name_01);

// here susie, sta1, name_1 are the variables

// to declare the variables in js , we use var, let and cont.
// the'var' keyword declare a variable with functional scope.
// the 'let' keyword declare a variable with block scope.
// the 'const' keyword declare a constant variable whose value cannot be change once it is assigned.